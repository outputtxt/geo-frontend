export default class CircularArea {
  constructor(X, Y, radius) {
    this.X = parseFloat(X); // center X coordinate
    this.Y = parseFloat(Y); // center Y coordinate
    this.radius = parseFloat(radius); // radius of the circle
  }
}
