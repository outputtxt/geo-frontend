export default class CellSorguResponse {
  constructor(cellId, bazX, bazY, angle, adres) {
    this.cellId = cellId;
    this.bazX = bazX;
    this.bazY = bazY;
    this.angle = angle;
    this.adres = adres;
  }
}
