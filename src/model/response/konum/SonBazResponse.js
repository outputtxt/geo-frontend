export default class SonBazResponse {
  constructor(tarih, cellId, bazX, bazY, angle, adres) {
    this.tarih = tarih;
    this.cellId = cellId;
    this.bazX = bazX;
    this.bazY = bazY;
    this.angle = angle;
    this.adres = adres;
  }
}
